set TEX_HOME "$HOME/Google Drive/我的雲端硬碟/miatex"

function _paper
  # fzf --multi \
  # peco --layout=bottom-up |read foo 
  peco --layout=bottom-up \
  | while read -l foo
  #   # TODO: collect all pdf files and tex files here for v and open command
    echo $fo
  end

  # echo (count $foo)
  # echo $foo

  # if  test (count $foo) -eq 0
  #   return 0 # return if press esc
  # end
  #
  # if test $foo = $TEX_HOME
  #   cd $TEX_HOME
  #   commandline -f repaint
  # else
  #   string match -rg '.*(?<cdpath>'$TEX_HOME'/[^/]+)/*(?<filepath>.*)' $foo --quiet
  #   # 1. change folder
  #   # printf 'cdpath: %s\n' $cdpath
  #   cd $cdpath
  #   # 2. execute command for different file
  #   # printf 'fileppath: %s\n' $filepath
  #   if string match -rg '.*\.tex$' $filepath
  #     command nvim $filepath
  #     commandline -f repaint
  #   else if string match -rg '.*\.pdf$' $filepath
  #     command open $filepath
  #     commandline -f repaint
  #   end
  # end
end

function paper
  begin
    if test -d $TEX_HOME
      ls -ad $TEX_HOME
      fd . -e tex -e pdf $TEX_HOME
    end
  end | _paper $argv
end
